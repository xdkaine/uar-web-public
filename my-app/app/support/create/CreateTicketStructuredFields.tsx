'use client';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { cn } from '@/lib/utils';
import type { TicketFormField, TicketFormValues } from '@/lib/support/ticket-form-schema';
export function TicketStructuredFields({ fields, values, setValues, missing, category }: { fields: TicketFormField[]; values: TicketFormValues; setValues: React.Dispatch<React.SetStateAction<TicketFormValues>>; missing: Set<string>; category: string }) {
  if (!fields.length) return null;
  return <fieldset className="space-y-4 rounded-lg border border-border bg-muted/30 p-4"><h2 className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">3 · Quick details</h2><div className="grid gap-4 sm:grid-cols-2">{fields.map((field) => { const value = values[field.key] ?? ''; const showError = field.required && missing.has(field.key) && Boolean(category || Object.keys(values).length); return <div key={field.key}><label htmlFor={`field-${field.key}`} className="mb-1.5 block text-sm font-medium text-foreground">{field.label} {field.required ? <span className="text-destructive">*</span> : <span className="font-normal text-muted-foreground">(optional)</span>}</label>{field.kind === 'select' ? <Select value={value} onValueChange={(next) => setValues((current) => ({ ...current, [field.key]: next }))}><SelectTrigger id={`field-${field.key}`} className={cn('w-full bg-background', showError && 'border-destructive')}><SelectValue placeholder={field.placeholder ?? 'Choose…'} /></SelectTrigger><SelectContent>{(field.options ?? []).map((option) => <SelectItem key={option.value} value={option.value}>{option.label}</SelectItem>)}</SelectContent></Select> : <input type="text" id={`field-${field.key}`} value={value} onChange={(event) => setValues((current) => ({ ...current, [field.key]: event.target.value }))} placeholder={field.placeholder} className={cn('w-full rounded-lg border border-input bg-background px-3 py-2 text-sm text-foreground focus:ring-2 focus:ring-ring', showError && 'border-destructive')} />}{field.help && <p className="mt-1 text-xs text-muted-foreground">{field.help}</p>}</div>; })}</div></fieldset>;
}
