import MetadataField from './RequestDetailField';
import type { AccessRequest } from './RequestDetailTypes';

export default function RequestApprovedDetails({ request, actorDisplayNames }: { request: AccessRequest; actorDisplayNames: Record<string, string> }) {
  if (request.status !== 'approved') return null;
  return <section className="border-t border-border pt-4 sm:pt-6"><h2 className="text-lg sm:text-xl font-semibold mb-3 sm:mb-4 text-foreground">Account Information</h2><dl className="grid grid-cols-1 md:grid-cols-2 gap-3 sm:gap-4">{request.ldapUsername && <MetadataField label="Domain Username" valueClassName="text-sm sm:text-base text-foreground font-semibold break-all">{request.ldapUsername}</MetadataField>}{request.vpnUsername && <MetadataField label="VPN Username" valueClassName="text-sm sm:text-base text-foreground font-semibold break-all">{request.vpnUsername}</MetadataField>}{request.acknowledgedAt && <MetadataField label="Acknowledged At">{new Date(request.acknowledgedAt).toLocaleString()}</MetadataField>}{request.acknowledgedBy && <MetadataField label="Acknowledged By" valueClassName="text-sm sm:text-base text-foreground font-semibold wrap-break-word"><span title={request.acknowledgedBy}>{actorDisplayNames[request.acknowledgedBy] || request.acknowledgedBy}</span></MetadataField>}{request.approvedAt && <MetadataField label="Approved At">{new Date(request.approvedAt).toLocaleString()}</MetadataField>}{request.approvedBy && <MetadataField label="Approved By" valueClassName="text-sm sm:text-base text-foreground font-semibold wrap-break-word"><span title={request.approvedBy}>{actorDisplayNames[request.approvedBy] || request.approvedBy}</span></MetadataField>}{request.accountExpiresAt && <MetadataField label="Account Disables">{new Date(request.accountExpiresAt).toLocaleString()}</MetadataField>}</dl></section>;
}
